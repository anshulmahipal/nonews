# mobile-legacy

This folder contains an older duplicate Expo shell that is no longer part of the active workspace.

Use `apps/expo` for all current mobile development.

This folder is kept only as temporary reference material while the repo is being cleaned up.
